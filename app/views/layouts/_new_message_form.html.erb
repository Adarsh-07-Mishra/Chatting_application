<!DOCTYPE html>
<html>
<head>
  <title>Styled Form</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    .msg-form {
      background-color: #f8f9fa; /* Light grey background */
      padding: 10px; /* Padding around the form */
      border-radius: 8px; /* Rounded corners */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    }

    .msg-content {
      flex: 1; /* Take up remaining space */
      border-radius: 20px; /* Rounded input field */
      padding: 10px 15px; /* Inner padding for the text field */
      border: 1px solid #ced4da; /* Border similar to Bootstrap's default */
      box-shadow: inset 0 1px 2px rgba(0, 0, 0, 0.1); /* Inner shadow for depth */
    }

    .msg-content:focus {
      border-color: #80bdff; /* Border color on focus */
      box-shadow: 0 0 5px rgba(0, 123, 255, 0.25); /* Outer glow on focus */
    }

    .btn-primary {
      border-radius: 20px; /* Rounded button */
      padding: 10px 20px; /* Padding for the button */
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="form-group msg-form">
      <%= form_with(model: [@single_room, @message], remote: true, class: "d-flex align-items-center w-100") do |f| %>
        <%= f.text_field :content, id: 'chat-text', class: "form-control msg-content me-2", placeholder: 'Type your message...', autocomplete: 'off' %>
        <%= f.submit 'Send', data: { disable_with: false }, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
