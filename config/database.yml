default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: chatting_pg
  username: chatting_pg
  password: chatting_pg
  host: localhost
  port: 5432

test:
  <<: *default
  database: chatting_pg
  username: chatting_pg
  password: chatting_pg
  host: localhost
  port: 5432

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
  # Remove database, username, password, host, and port, as they will be pulled from DATABASE_URL
